<h2><%=current_user.name%> Page</h2>

<h3><%=link_to "Explore Activities", activities_path%></h3>

Ratings:

<ul>

    <%current_user.ratings.each do |rating|%>
        <li><%=link_to "#{rating.item_model_characteristic.item_model.name}", item_model_path(rating.item_model_characteristic.item_model)%>
        - <%=link_to "#{rating.item_model_characteristic.characteristic.name}", characteristic_path(rating.item_model_characteristic.characteristic)%> - 
        <%=link_to "#{rating.rating}", rating_path(rating)%></li>
    <%end%>

</ul>