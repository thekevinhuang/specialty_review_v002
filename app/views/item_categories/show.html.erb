<h2><%=@item_category.name%></h2>

<h3><%=@item_category.description%></h3>

<h3>Belongs to: <%=link_to @item_category.activity.name, activity_path(@item_category.activity)%></h3>

<h3>Specific Models:</h3>

<%=link_to "Add a new Model", new_item_category_item_model_path(@item_category)%>

<ul>
    <%@item_category.item_models.each do |item_model|%>
        <%=link_to item_model.name, item_model_path(item_model)%><br>
    <%end%>
</ul>

